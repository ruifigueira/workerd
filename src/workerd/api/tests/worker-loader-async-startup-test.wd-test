using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [(
    name = "worker-loader-async-startup-test",
    worker = (
      modules = [
        (name = "worker", esModule = embed "worker-loader-async-startup-test.js"),
        (name = "add.wasm.bin", data = embed "test.wasm"),
      ],
      compatibilityFlags = ["nodejs_compat", "rpc", "enable_ctx_exports", "experimental"],
      bindings = [(name = "loader", workerLoader = ())],
    ),
  )],
);
